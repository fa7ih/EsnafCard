@{
    ViewData["Title"] = "Kart Oluştur";
}

<h2 class="mb-4">
    <i class="bi bi-credit-card-2-front"></i> Yeni Kart Oluştur
</h2>

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Tek Kart Oluştur</h5>
            </div>
            <div class="card-body">
                <form method="post" asp-action="CreateCard">
                    <div class="mb-3">
                        <label class="form-label">
                            <i class="bi bi-cash-stack"></i> Başlangıç Bakiyesi (TL)
                        </label>
                        <input type="number" name="initialBalance" class="form-control" 
                               step="0.01" min="0" required value="1000">
                    </div>

                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-plus-circle"></i> Kart Oluştur
                    </button>
                </form>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-success text-white">
                <h5 class="mb-0">Toplu Kart Oluştur</h5>
            </div>
            <div class="card-body">
                <form method="post" asp-action="CreateMultipleCards">
                    <div class="mb-3">
                        <label class="form-label">
                            <i class="bi bi-stack"></i> Kart Sayısı
                        </label>
                        <input type="number" name="count" class="form-control" 
                               min="1" max="100" required value="10">
                        <small class="text-muted">En fazla 100 kart oluşturabilirsiniz</small>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">
                            <i class="bi bi-cash-stack"></i> Her Kart İçin Başlangıç Bakiyesi (TL)
                        </label>
                        <input type="number" name="initialBalance" class="form-control" 
                               step="0.01" min="0" required value="1000">
                    </div>

                    <button type="submit" class="btn btn-success w-100">
                        <i class="bi bi-lightning-fill"></i> Toplu Oluştur
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="card mt-4">
    <div class="card-header bg-info text-white">
        <h5 class="mb-0">
            <i class="bi bi-info-circle"></i> Bilgilendirme
        </h5>
    </div>
    <div class="card-body">
        <ul>
            <li>Kart numaraları sistem tarafından otomatik olarak 8 haneli rastgele üretilir</li>
            <li>Her kart numarası benzersizdir ve tekrar üretilemez</li>
            <li>Oluşturulan kartlar varsayılan olarak aktif durumda olacaktır</li>
            <li>Kart bakiyelerini daha sonra güncelleyebilirsiniz</li>
            <li>Toplu oluşturma işleminde aynı bakiye ile birden fazla kart oluşturulur</li>
            <li><strong>Önemli:</strong> Oluşturduğunuz kartlar sadece sizin hesabınıza ait olacaktır</li>
        </ul>
    </div>
</div>

<div class="mt-3">
    <a href="/User/CardList" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Kart Listesine Dön
    </a>
</div>
